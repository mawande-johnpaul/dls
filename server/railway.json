{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": ["server/**"]
  },
  "deploy": {
    "startCommand": "gunicorn --pythonpath server server.wsgi",
    "rootDirectory": "server"
  }
}